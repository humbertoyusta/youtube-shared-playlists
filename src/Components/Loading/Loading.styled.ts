import styled from "styled-components";

export const LoadingWrapperStyled = styled.div`
    position: relative;
`;

export const LoadingAnimationStyled = styled.img`
    width: 64px;
    height: 64px;
    position: relative;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
`;